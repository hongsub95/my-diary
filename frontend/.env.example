# 로컬 FastAPI 주소. 운영에서는 같은 origin의 /api/v1을 기본으로 사용한다.
VITE_API_BASE_URL=http://localhost:8000/api/v1

# 카카오맵 JavaScript 키. 이 값은 브라우저에 그대로 노출되므로 REST 키를 넣으면 안 된다.
# 비워두면 지도 자리에 안내만 뜨고 장소 목록은 그대로 동작한다.
# 카카오 개발자 콘솔의 웹 플랫폼 사이트 도메인에 이 앱의 주소를 등록해야 지도가 뜬다.
VITE_KAKAO_JAVASCRIPT_KEY=40cfc38f75df3ee783afebef00080421
