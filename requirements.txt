fastapi==0.115.12
uvicorn[standard]==0.34.2
pydantic-settings==2.9.1
# Pydantic의 EmailStr 타입이 이 패키지를 필요로 한다.
email-validator==2.2.0

# Database
sqlalchemy==2.0.41
psycopg2-binary==2.9.10
alembic==1.16.2

# Auth
# passlib은 2020년 이후 유지보수가 멈춰 bcrypt 5.x와 호환되지 않으므로 bcrypt를 직접 사용한다.
# python-jose 역시 유지보수가 멈추고 알려진 취약점이 있어 PyJWT를 사용한다.
bcrypt==5.0.0
pyjwt==2.10.1
# 웹 세션 저장소 (앱은 JWT, 웹은 Redis 세션을 사용한다)
redis==5.2.1

# 파일 업로드
# FastAPI가 multipart/form-data(사진 업로드)를 파싱할 때 필요하다. 없으면 해당
# 엔드포인트가 요청을 받는 순간 500으로 떨어진다.
python-multipart==0.0.20

# 시간대
# 날짜 조회 파라미터를 한국 시간 하루로 해석할 때 zoneinfo가 IANA tz 데이터를 찾는다.
# 리눅스에는 보통 OS에 들어 있지만 Windows에는 없어서, 없으면 ZoneInfoNotFoundError가 난다.
tzdata==2025.2

# Test
pytest==8.3.5
httpx==0.28.1
